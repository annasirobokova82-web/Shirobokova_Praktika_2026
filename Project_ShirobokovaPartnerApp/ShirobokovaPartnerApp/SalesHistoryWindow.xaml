<Window x:Class="ShirobokovaPartnerApp.SalesHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История продаж - Широбокова А.М."
        Height="450" Width="700"
        WindowStartupLocation="CenterOwner"
        MinHeight="300" MinWidth="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="История реализации продукции" 
                           FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock x:Name="PartnerNameText" FontSize="12" Foreground="LightGray" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Статистика -->
        <Border Grid.Row="1" Background="#ECF0F1" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="TotalSalesText" FontWeight="Bold" Foreground="#27AE60"/>
            </StackPanel>
        </Border>

        <!-- Таблица продаж -->
        <DataGrid x:Name="SalesGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" RowHeaderWidth="0"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата продажи" Binding="{Binding SaleDate, StringFormat=dd.MM.yyyy}" Width="120"/>
                <DataGridTextColumn Header="Наименование продукции" Binding="{Binding Product.Name}" Width="250"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Сумма (руб.)" Binding="{Binding TotalAmount, StringFormat=N2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопка закрытия -->
        <Button Grid.Row="3" Content="Закрыть" Click="BtnClose_Click" 
                Width="100" Height="35" Margin="0,10,0,0" 
                HorizontalAlignment="Center" Background="#3498DB" Foreground="White"/>
    </Grid>
</Window>